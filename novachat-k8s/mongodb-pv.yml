apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pv
  namespace: novachat
  labels:
    app: mongodb
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  hostPath:
    path: /data/mongodb